{
  "scripts": {
    "build": "esbuild lib/client.tsx --bundle --outfile=public/bundle.js",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.9",
    "esbuild": "^0.16.4"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
